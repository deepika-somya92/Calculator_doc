sudo: required

language: go

go:
  - 1.7

services:
  - docker

after_success:
  - docker build -t "fedolin/calculator:latest" .
  - docker login -u=fedolin -p=dockerfedo
  - docker push fedolin/calculator

